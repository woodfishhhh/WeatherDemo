name: GitHub Pages Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment is disabled for this project. Deploy to the Ubuntu server instead."
